import { PatchPlayerRequest } from './patch-player-request';

describe('PatchPlayerRequest', () => {
  it('should create an instance', () => {
    expect(new PatchPlayerRequest()).toBeTruthy();
  });
});
